org.gradle.jvmargs=-Xmx3g -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# Public release-manifest repository for the in-app updater (see docs/UPDATES.md). This is build
# configuration compiled into BuildConfig.UPDATE_REPO, not a credential; no token is stored here.
rollora.updateRepo=MirFaizan06/Rollora
